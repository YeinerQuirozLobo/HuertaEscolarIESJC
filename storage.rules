rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {

    // Solo permitir subir a /products/<uid>/...
    match /products/{userId}/{allPaths=**} {
      allow read: if true; // todos pueden ver imágenes
      allow write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
